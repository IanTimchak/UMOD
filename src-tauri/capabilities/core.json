{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "core",
  "description": "core capability for a window",
  "windows": ["dictionary-window-*"],
  "permissions": [
    "core:default",
    "opener:default"
    
  ]
}
